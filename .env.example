NODE_ENV=development
PORT=3000

# Optional. If omitted, the API uses an in-memory repository for local demos/tests.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/logistics_agents

# Optional Fleetbase API settings.
FLEETBASE_BASE_URL=https://api.fleetbase.io
FLEETBASE_API_KEY=replace-me
